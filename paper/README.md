# Paper

No manuscript has been started.

Create the LaTeX manuscript only after the baseline model is stable enough that
the MASTER memo can be translated without carrying unresolved alternatives into
the main text. Keep one build target, use project-relative paths, and place
generated tables and figures under `../outputs/`.

On this machine, compile with the configured MiKTeX installation and verify the
rendered PDF before treating the manuscript as complete.
